# app/data/constants.py

from typing import Dict

# Define the Endpoint type (if necessary)
Endpoint = str

# Endpoint IDs mapping
endpointIds: Dict[Endpoint, int] = {

    "License Plate Detector": 5,
}
